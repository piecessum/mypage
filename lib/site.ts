/**
 * Внешние ссылки. Здесь же — перелинковка с личным сайтом (для друзей).
 *
 * TODO: подставь реальный домен после деплоя на Vercel.
 * Можно переопределить через переменную окружения NEXT_PUBLIC_FRIENDS_URL,
 * не трогая код.
 */
export const FRIENDS_URL =
  process.env.NEXT_PUBLIC_FRIENDS_URL ?? "https://alex-friends.vercel.app";
